import StandaloneShell from '@/components/StandaloneShell';

export const metadata = {
  title: 'Workflow — Merlin Studio',
};

export default function WorkflowTabPage() {
  return <StandaloneShell />;
}
