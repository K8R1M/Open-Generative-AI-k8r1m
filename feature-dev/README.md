# feature-dev

Single live plan: **`merlin-studio-plan/`** — start at `merlin-studio-plan/00-README.md`
(note its PLAN STATUS line), then `task-list.md`, then `execution-log.md`.

Everything else that ever lived here is superseded and moved to `_archive/`
(dated folders). Do not read `_archive/` unless explicitly asked — everything
needed from it is distilled into the live plan.
